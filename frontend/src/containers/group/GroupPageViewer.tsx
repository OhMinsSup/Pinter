import * as React from 'react';
import GroupHeader from 'src/components/group/GroupHeader';

class GroupPageViewer extends React.Component<{}> {
    public render() {
        return (
            <React.Fragment>
                <GroupHeader />
            </React.Fragment>
        )
    }
}

export default GroupPageViewer;