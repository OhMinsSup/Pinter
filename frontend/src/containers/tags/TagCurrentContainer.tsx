import * as React from 'react';
import TagCurrent from '../../components/tags/TagCurrent';

class TagCurrentContainer extends React.Component {
    public render() {
        return (
            <TagCurrent />
        );
    }
}

export default TagCurrentContainer;